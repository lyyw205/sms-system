# SMS 예약 시스템 - Demo/MVP 버전

클라이언트 시연용 데모 시스템 - 모든 외부 API 모킹 + 핫스왑 구조

## 🎯 개요

- **목적**: 클라이언트에게 시스템 가치를 시연하고 계약 후 즉시 프로덕션 전환 가능
- **데모 모드**: 모든 외부 API(SMS, 네이버 예약, Google Sheets, Claude) 모킹
- **핫스왑**: 환경변수(`DEMO_MODE=false`)만 변경하면 프로덕션 모드로 전환
- **프로덕션 전환 시간**: 9시간 (실제 API 연동)

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│           웹 대시보드 (React + Ant Design)                 │
│  Dashboard | Reservations | Messages | Rules | Documents │
└───────────────────────┬─────────────────────────────────┘
                        │ REST API
┌───────────────────────▼─────────────────────────────────┐
│                FastAPI 백엔드 서버                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │         추상화 레이어 (Protocol Interface)       │   │
│  │  - SMSProvider, LLMProvider, etc.              │   │
│  └─────────────┬───────────────────────────────────┘   │
│                │                                         │
│       ┌────────▼────────┐       ┌──────────────────┐   │
│       │ Mock 구현        │       │ Real 구현         │   │
│       │ (DEMO_MODE=true) │      │ (DEMO_MODE=false) │   │
│       └─────────────────┘       └──────────────────┘   │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │   PostgreSQL + Redis + ChromaDB                    │ │
│  └────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

## 📦 주요 기능

### 1. SMS 자동 응답 시스템
- ✅ 룰 기반 응답 (정규식 패턴 매칭, 신뢰도 95%)
- ✅ LLM 폴백 (Mock: 키워드 매칭 / Real: Claude API + RAG)
- ✅ Human-in-the-Loop (신뢰도 낮은 응답은 검토 대기 큐)
- ✅ 자동 발송 (신뢰도 60% 이상)

### 2. 예약 관리
- ✅ 예약 CRUD (생성, 조회, 수정, 삭제)
- ✅ 네이버 예약 동기화 (Mock: JSON 파일 / Real: API/크롤링)
- ✅ Google Sheets 연동 (Mock: CSV 파일 / Real: Sheets API)
- ✅ 예약 상태 변경 시 자동 SMS 알림

### 3. 웹 대시보드
- ✅ 통계 카드 (예약 수, 메시지 수, 자동 응답률)
- ✅ 차트 (응답 유형 분포 Pie Chart)
- ✅ 최근 활동 (예약/메시지 테이블)
- ✅ SMS 수신 시뮬레이터 (데모 시연용)

### 4. 룰 및 문서 관리
- ✅ 룰 CRUD (패턴, 응답, 우선순위, 활성화)
- ✅ 문서 업로드 (Mock: 메타데이터만 / Real: ChromaDB RAG 인덱싱)

## 🚀 빠른 시작

### 1. Docker 컨테이너 시작

```bash
cd sms-reservation-system
docker compose up -d
```

PostgreSQL, Redis, ChromaDB 컨테이너 시작

### 2. 백엔드 실행

```bash
cd backend

# Python 가상환경 생성 및 활성화
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt

# 데이터베이스 초기화 및 시드 데이터 생성
python -m app.db.seed

# API 서버 실행
uvicorn app.main:app --reload
```

API 서버: http://localhost:8000
Swagger UI: http://localhost:8000/docs

### 3. 프론트엔드 실행

```bash
cd frontend

# 의존성 설치
npm install

# 개발 서버 실행
npm run dev
```

프론트엔드: http://localhost:5173

## 🎬 클라이언트 시연 시나리오 (10분)

### 1. 대시보드 Overview (1분)
- URL: http://localhost:5173
- 통계 카드, 차트, 최근 활동 확인

### 2. SMS 수신 시뮬레이션 (2분)
- Messages 페이지로 이동
- SMS 시뮬레이터에서:
  - 발신자: `010-1234-5678`
  - 메시지: `영업시간이 어떻게 되나요?`
  - "수신 시뮬레이션" 클릭
- **데모 포인트**:
  - 메시지 목록에 즉시 표시
  - 터미널 로그에서 `[MOCK SMS SENT]` 확인
  - "실제로는 여기서 SMS가 발송됩니다" 설명

### 3. 자동 응답 확인 (2분)
- 룰 매칭: "영업시간" 키워드 → 자동 응답
- LLM 폴백: "할인 있나요?" → Mock LLM 응답
- 검토 대기: 신뢰도 낮은 응답은 "검토 대기" 탭에 표시

### 4. 예약 관리 (2분)
- Reservations 페이지
- "신규 예약" 클릭 → 폼 입력 → 저장
- 예약 상태 변경 (pending → confirmed)
- **데모 포인트**: 터미널 로그에서 SMS 알림 발송 확인

### 5. 동기화 기능 (2분)
- "네이버 예약 동기화" 클릭 → JSON 파일에서 2개 예약 추가
- "Google Sheets 동기화" 클릭 → CSV 파일 생성
- **데모 포인트**:
  - `backend/app/mock/data/reservations.csv` 파일 열어서 보여주기
  - "실제로는 Google Sheets에 연동됩니다" 설명

### 6. 룰 및 문서 관리 (1분)
- Rules 페이지: "비즈니스 요구에 맞게 자유롭게 추가/수정"
- Documents 페이지: "문서 업로드 시 RAG가 학습합니다"

## 🔑 핵심 파일 (핫스왑 구조)

### 1. `/backend/app/config.py`
**역할**: DEMO_MODE 환경변수 관리

```python
class Settings(BaseSettings):
    DEMO_MODE: bool = True  # ← 이 값으로 모드 전환!
    SMS_API_KEY: str = ""
    CLAUDE_API_KEY: str = ""
    # ...
```

### 2. `/backend/app/factory.py`
**역할**: Provider 팩토리 패턴 - Mock과 Real 자동 교체

```python
def get_sms_provider() -> SMSProvider:
    if settings.DEMO_MODE:
        return MockSMSProvider()  # Demo mode
    else:
        return RealSMSProvider(api_key=settings.SMS_API_KEY)  # Production
```

### 3. `/backend/app/providers/base.py`
**역할**: 추상화 레이어 (Protocol 정의)

```python
class SMSProvider(Protocol):
    async def send_sms(self, to: str, message: str) -> Dict[str, Any]: ...
```

## 🔄 프로덕션 전환 가이드

### 1단계: 환경변수 설정

```bash
# backend/.env 수정
DEMO_MODE=false

# API 키 입력
SMS_API_KEY=your_sms_api_key
CLAUDE_API_KEY=your_claude_api_key
GOOGLE_SHEETS_CREDENTIALS=/path/to/service_account.json
NAVER_RESERVATION_EMAIL=your_email
NAVER_RESERVATION_PASSWORD=your_password
```

### 2단계: Real Provider 구현 (9시간)

| Provider | 작업 | 예상 시간 |
|----------|------|----------|
| RealSMSProvider | NHN Cloud API 연동, 웹훅 설정 | 1시간 |
| RealReservationProvider | 네이버 API/크롤링 | 2시간 |
| RealStorageProvider | Google Sheets API | 1시간 |
| RealLLMProvider | Claude API + RAG (ChromaDB) | 3시간 |
| 통합 테스트 | 전체 플로우 검증 | 2시간 |

### 3단계: 서버 재시작

```bash
docker compose restart
uvicorn app.main:app --reload
```

팩토리가 자동으로 Real Provider 사용! 로그에서 `🚀 Using RealSMSProvider` 확인

## 📊 데이터 구조

### 데이터베이스 (PostgreSQL)
- **messages**: SMS 메시지 이력
- **reservations**: 예약 정보
- **rules**: 자동 응답 룰
- **documents**: 지식베이스 문서

### Mock 데이터 파일
- `backend/app/mock/data/naver_reservations.json`: 샘플 네이버 예약 2개
- `backend/app/mock/data/reservations.csv`: Google Sheets 동기화 결과
- `backend/app/rules/rules.yaml`: 기본 룰 5개

## 🚨 시연 시 주의사항

### ✅ 투명성
- "현재는 데모 모드로, 실제 API는 연동되지 않았습니다"
- "모든 외부 API는 모킹되어 비용이 발생하지 않습니다"

### ✅ 전환 가능성 강조
- "계약 후 환경변수와 API 키만 입력하면 9시간 내 프로덕션 전환"
- 터미널에서 `.env` 파일 보여주며 "DEMO_MODE를 false로 바꾸고 API 키 입력"

### ✅ 핫스왑 구조
- 코드 에디터에서 `factory.py` 보여주며 "팩토리 패턴으로 자동 교체"
- "동일한 인터페이스로 Mock/Real 교체 가능"

## 📝 API 엔드포인트

### Messages
- `GET /api/messages`: 메시지 목록
- `POST /api/messages/send`: 수동 SMS 발송
- `GET /api/messages/review-queue`: 검토 대기 큐
- `POST /webhooks/sms/receive`: SMS 수신 (시뮬레이션)

### Reservations
- `GET /api/reservations`: 예약 목록
- `POST /api/reservations`: 예약 생성
- `PUT /api/reservations/{id}`: 예약 수정
- `DELETE /api/reservations/{id}`: 예약 삭제
- `POST /api/reservations/sync/naver`: 네이버 동기화
- `POST /api/reservations/sync/sheets`: Google Sheets 동기화

### Auto-Response
- `POST /api/auto-response/generate`: 자동 응답 생성
- `POST /api/auto-response/test`: 응답 테스트
- `POST /api/auto-response/reload-rules`: 룰 리로드

### Rules & Documents
- `GET/POST/PUT/DELETE /api/rules`: 룰 CRUD
- `GET /api/documents`: 문서 목록
- `POST /api/documents/upload`: 문서 업로드
- `DELETE /api/documents/{id}`: 문서 삭제

### Dashboard
- `GET /api/dashboard/stats`: 대시보드 통계

## 🛠️ 기술 스택

### Backend
- FastAPI (Python)
- SQLAlchemy + PostgreSQL
- Redis (큐/캐시)
- ChromaDB (RAG 벡터DB)
- Pydantic (설정 관리)

### Frontend
- React + TypeScript
- Ant Design (UI 컴포넌트)
- React Router (라우팅)
- Recharts (차트)
- Axios (HTTP 클라이언트)

### Infrastructure
- Docker Compose
- PostgreSQL 15
- Redis 7
- ChromaDB (latest)

## 📚 추가 자료

- Swagger UI: http://localhost:8000/docs
- 시드 데이터: `backend/app/db/seed.py`
- 룰 정의: `backend/app/rules/rules.yaml`
- Mock 데이터: `backend/app/mock/data/`

## ✅ 최종 체크리스트

### 데모 버전 완성
- [x] Phase 1: 기반 구축 + 모킹 레이어
- [x] Phase 2: 자동 응답 엔진 (룰 + Mock LLM)
- [x] Phase 3: 예약 연동 + 자동 알림
- [x] Phase 4: 웹 대시보드 (5개 페이지)
- [x] Mock 동작 확인 (로그 출력)
- [x] 시연 시나리오 준비

### 프로덕션 전환 준비
- [x] `.env.example` 완성
- [x] Real Provider 스텁 준비
- [x] 프로덕션 전환 가이드
- [ ] API 키 획득 (계약 후)

## 📞 문의

시스템 관련 문의사항은 개발팀으로 연락 주세요.

---

**🎉 Demo Mode로 클라이언트에게 가치를 증명하고, 계약 후 9시간 내 프로덕션 전환!**
